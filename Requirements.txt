Requirements:

Python >= 3.12.3
PyNaCl >= 1.6.2
pycparser == 3.0
cffi == 2.0.0

If wheel fails to build for PyNaCl "ERROR: Could not build wheels for pynacl, which is required to install pyproject.toml-based projects"
> Try `pip install pynacl --only-binary :all:` 
